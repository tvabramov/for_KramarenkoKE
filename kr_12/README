DESCRIPTION:

Text chat via IPC (Message Queues) mechanism.

USING:

Create server with name "Server": mqchat -s -n "Server"
Create client with name "Client": mqchat -n "Client"

HELP:

https://www.opennet.ru/base/dev/ipc_msg.txt.html
