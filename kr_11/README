Directory and file viewer + file editor + file runner + piped file runner (kr_6's child project)

To edit a file, press "Enter", when you can replace its characters.
Working with files is done via mapping files into memory - mmap and msync functions.
To Apply changes and save file, press F6 key.

If you want to execute program, press F7 key. In this folder there is "test" program, that
returns random value, you can test programs launching with it.

If you want to run piped, "ls -1 | head -n 5", for example, use F8 key.

